# 2026-05-14 N5 CI Gate

## Purpose

This note closes the local repository part of N5 from the Claude privacy audit: the frontend operator-env exposure check now has a CI owner.

## Current State

Status: `ci-verified-pushed-live`

The repository now includes `.github/workflows/privacy-audit.yml`, which runs on pull requests and pushes to `main` or `codex/**` branches.

The workflow runs:

- `npm ci`
- `npm run privacy-audit:tracker-check`
- `npm run frontend:operator-env-exposure-check`

The frontend operator-env exposure command runs the production build internally, then scans the generated bundle for forbidden operator env names, token-shaped env exposure, and keypair-shaped material.

## Verification

Local verification passed:

- `npm run frontend:operator-env-exposure-check`
- `npm run privacy-audit:tracker-check`

Hosted CI verification passed:

- GitHub Actions `Vanta Privacy Audit Gates` on `main`, run `25888198149`, commit `7635c9b641a505379b82e74a6679986dbabc55fe`.
- GitHub Actions `Vanta Privacy Audit Gates` on `codex/vanta-zk-review-hardening`, run `25888193053`, commit `7635c9b641a505379b82e74a6679986dbabc55fe`.

## Remaining Evidence

None for the referenced N5 receipt. The CI gate is verified for the pushed/live website receipt commit. This does not imply production privacy, on-chain proof verification, SBF redeploy, or private-settlement readiness; rerun GitHub Actions for newer commits.
