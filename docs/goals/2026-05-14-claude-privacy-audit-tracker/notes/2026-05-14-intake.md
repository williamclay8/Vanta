# 2026-05-14 Intake - Claude Privacy Audit

## Decision

Treat the audit as a long-lived implementation tracker, not a one-time review note. The immediate local work is:

1. Keep website truthfulness conservative at crawler, landing, and transaction surfaces.
2. Close the new circuit soundness findings N1 and N2 before any proof-verifier or production-private claim work.
3. Preserve on-chain verifier, custody, root, deploy, recipient discovery, and anonymity-set work as explicit blockers with positive verification gates.

## Live Website Evidence

Clay provided direct live evidence after the initial audit:

- `https://vantaprivacy.xyz` head and deployed dist bundle were read directly.
- The deployed bundle has 62 JS chunks, about 2.7 MB, with hashed filenames matching live `index.html`.
- The local/live bundle hash match cited was `index-BhWFlXXv.js`.
- Landing route banned-claim scan found no positive `fully private`, `anonymous payments`, `untraceable`, `production-ready`, or `live mainnet private settlement` claims.
- Public `/.well-known/vanta-audit.json` remains fail-closed: `productionReady: false`, `mainnetReady: false`, `liveDeploymentVerified: false`, `privacyClaimAllowed: false`, `anonymityClaimAllowed: false`, `solanaC01Groth16VerifierReady: false`, and `productionVerifierBackendSelected: false`.
- Live anonymity set evidence: `currentDistinctCommitments: 2`, `minimumDistinctCommitments: 1024`.
- Live bundle scan found no `VITE_OPERATOR_*`, token-shaped env exposure, third-party analytics scripts, or long base58 keypair-shaped strings.

Conclusion: deployed website copy is verified conservative except the crawler-visible meta description, and product transaction pages still needed explicit in-flow caveats for operator vault custody, self-wallet exit, and browser-local recovery.

## N1-N5 Mapping

| Source label | Status | Repo lane | Next proof |
|---|---|---|---|
| N1 | Local implemented | PPv2 Shield circuit output commitment binding | Invalid-preimage fixture fails; `npm run private-pool-v2:shield-circuit-check` |
| N2 | Partial local implementation | PPv2 Send/Claim/Swap/Spend plus Private Core Send/Swap owner binding | PPv2 Send and Claim owner/input-binding fixtures fail correctly; Swap owner-binding fixture fails correctly; Swap input preimage, Spend, and Private Core Send/Swap remain |
| N3 | Local implemented | Meta description and product caveats | `npm run truth:privacy-claim-gate`; page-specific checks; build |
| N4 | Blocked approval-gated | SBF redeploy/live lineage | SBF ABI check plus explicit deploy/live receipts after approval |
| N5 | Pending CI | Frontend operator env exposure pre-deploy gate | `npm run frontend:operator-env-exposure-check` in build/CI |

## Lumi Hygiene

- Local: N1/N3 implementation receipt exists in this tracker; use `git status` for current dirt.
- Committed: implementation commit `86497bd` records the tracker, N3 truth fixes, and N1 Shield output-commitment preimage binding.
- Pushed: `codex/vanta-zk-review-hardening` has been used as the publishing branch; check git refs for the exact pushed head.
- Deployed/live: the user supplied live-read evidence for the old deployed bundle; these changes are not live until the default branch is updated, Render deploys, and the public website is reverified.

## Local Implementation Receipt

Commit `86497bd` ("Harden privacy audit truth gates") records:

- N1: `zk/noir/vanta_private_pool_v2_shield_entry/src/main.nr` now binds `output_commitment` to `owner_commitment`, `target_asset_id`, `amount`, `output_blinding`, and `output_derivation_tag`.
- N1: `scripts/check-vanta-private-pool-v2-shield-circuit.mjs` now runs `invalid-output-commitment-preimage` and observes the expected circuit failure.
- N3: `index.html` meta description is beta-truthful and points at `/.well-known/vanta-audit.json`.
- N3: Shield, Unshield, and recovery surfaces now disclose operator-controlled vault custody, self-wallet exit, and browser-local recovery risk at the action surfaces.

Verification observed locally:

- `npm run privacy-audit:tracker-check`
- `npm run zk:circuit-soundness-lint`
- `npm run private-pool-v2:shield-circuit-check`
- `npm run private-pool-v2:shield-proof-request-check`
- `npm run private-pool-v2:public-input-hash-alignment-check`
- `npm run private-pool-v2:shield-browser-worker-prover-check`
- `npm run private-pool-v2:send-circuit-check`
- `npm run private-pool-v2:send-proof-request-check`
- `npm run private-pool-v2:send-witness-prover-check`
- `npm run private-pool-v2:browser-worker-prover-check`
- `npm run private-pool-v2:send-proof-artifact-consistency-check`
- `npm run private-pool-v2:send-operator-no-witness-check`
- `npm run private-pool-v2:send-prove`
- `npm run private-pool-v2:public-input-hash-alignment-check`
- `npm run shield:ui-claim-boundary-check`
- `npm run unshield:public-exit-surface-check`
- `npm run recovery:panel-check`
- `npm run recovery:settings-route-check`
- `npm run truth:privacy-claim-gate`
- `npm run zk:feedback-loop-check`
- `npm run mainnet:render-workspace-check`
- `npm run build`
- `git diff --cached --check`

## N2 Send Slice Receipt

This slice closes the Private Pool v2 Send portion of N2:

- `zk/noir/vanta_private_pool_v2_send_entry/src/main.nr` now asserts `owner_commitment = poseidon1([owner_secret])`.
- The same circuit now asserts `input_commitment = poseidon5([owner_commitment, asset_id_commitment, input_amount, input_blinding, input_derivation_tag])`.
- `src/privacy/privatePoolV2SendCircuitFixture.ts` now derives default Send input commitments from that preimage and rejects forged owner/input preimages in typed witness inputs.
- Red-first evidence: `npm run private-pool-v2:send-circuit-check` failed until the invalid owner/input fixture modes and preimage constraints were wired together, then passed with both expected failures observed.
- Debugging note: `private-pool-v2:browser-worker-prover-check` initially failed when it was run in parallel with another command that rewrites `Prover.toml`. The fix was to avoid racing Prover writers and update the stale Send witness-input builders for the new fields.

Remaining N2 scope:

- PPv2 Swap-to-shielded input-preimage design, actual-private-spend, and Private Core Send/Swap need explicit key-model/input-preimage decisions before broad implementation.

## N2 Swap-To-Shielded Owner Slice Receipt

This slice closes the Private Pool v2 Swap-to-shielded owner-commitment portion of N2:

- `zk/noir/vanta_private_pool_v2_swap_to_shielded_entry/src/main.nr` now asserts `owner_commitment = poseidon1([owner_secret])`.
- `src/privacy/privatePoolV2SwapToShieldedCircuitFixture.ts` now derives the default Swap owner commitment from `owner_secret` and rejects forged owner commitments in typed witness inputs.
- Red-first evidence: `npm run private-pool-v2:swap-to-shielded-circuit-check` failed while `invalid-owner-secret-binding` still solved, then passed after the owner-preimage constraint and fixture mode were wired together.
- Verification observed locally: `npm run private-pool-v2:swap-to-shielded-circuit-check`, `npm run zk:circuit-soundness-lint`, `npx tsc --noEmit --pretty false`, `npm run private-pool-v2:swap-to-shielded-proof-request-check`, `npm run private-pool-v2:swap-to-shielded-browser-worker-prover-check`, `npm run private-pool-v2:swap-to-shielded-proof-artifact-consistency-check`, `npm run private-pool-v2:swap-to-shielded-operator-no-witness-check`, and `npm run private-pool-v2:swap-to-shielded-prove`.
- Boundary: Swap-to-shielded input-commitment preimage binding remains a protocol design decision because the current circuit ABI exposes opaque `settlement_commitment`, `route_commitment`, and `economics_commitment`, not a clear raw input asset/amount preimage.

Remaining N2 scope:

- PPv2 Swap-to-shielded input-preimage design, actual-private-spend, and Private Core Send/Swap need explicit key-model/input-preimage decisions before broad implementation.

## N2 Claim Slice Receipt

This slice closes the Private Pool v2 Claim portion of N2:

- `zk/noir/vanta_private_pool_v2_claim_entry/src/main.nr` now asserts `owner_commitment = poseidon1([owner_secret])`.
- The same circuit now asserts `input_commitment = poseidon5([owner_commitment, asset_id, amount, input_blinding, input_derivation_tag])`.
- `src/privacy/privatePoolV2ClaimCircuitFixture.ts` now derives default Claim input commitments from that preimage and rejects forged owner/input preimages in typed witness inputs.
- Red-first evidence: `npm run private-pool-v2:claim-circuit-check` failed while `invalid-owner-secret-binding` still solved, then passed after the preimage constraints and fixture modes were wired together.
- Verification observed locally: `npm run private-pool-v2:claim-circuit-check`, `npm run zk:circuit-soundness-lint`, `npx tsc --noEmit --pretty false`, `npm run private-pool-v2:claim-proof-request-check`, `npm run private-pool-v2:claim-browser-worker-prover-check`, `npm run private-pool-v2:claim-proof-artifact-consistency-check`, and `npm run private-pool-v2:claim-operator-no-witness-check`.

Remaining N2 scope:

- PPv2 Swap-to-shielded, actual-private-spend, and Private Core Send/Swap need explicit key-model/input-preimage decisions before broad implementation.
